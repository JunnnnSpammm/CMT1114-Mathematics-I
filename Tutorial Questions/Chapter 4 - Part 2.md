1. Sketch the graph of each function by transforming the graph of an appropriate function of the form $y = x^n$. Indicate all x- and y-intercepts on each graph.
    - $P(x) = x^2 - 4$, $Q(x) = (x - 4)^2$, $R(x) = 2x^2 - 2$, $S(x) = 2(x - 2)^2$
    - $P(x) = x^4 - 16$, $Q(x) = (x + 2)^4$, $R(x) = (x + 2)^4 - 16$, $S(x) = -2(x + 2)^4$
    - $P(x) = x^3 - 8$, $Q(x) = -x^3 + 27$, $R(x) = -(x + 2)^3$, $S(x) = \frac{1}{2}(x - 1)^3 + 4$
    - $P(x) = (x + 3)^5$, $Q(x) = 2(x + 3)^5 - 64$, $R(x) = -\frac{1}{2}(x - 2)^5$, $S(x) = -\frac{1}{2}(x - 2)^5 + 16$

2. Match the polynomial function with one of the graphs I--VI. Give reasons for your choice.
    - $P(x) = x(x^2 - 4)$
    - $Q(x) = -x^2(x^2 - 4)$
    - $R(x) = -x^5 + 5x^3 - 4x$
    - $S(x) = \frac{1}{2}x^6 - 2x^4$
    - $T(x) = x^4 + 2x^3$
    - $U(x) = -x^3 + 2x^2$

3. Sketch the graph of the polynomial function. Make sure your graph shows all intercepts and exhibits the proper end behavior.
    - $P(x) = (x - 1)(x + 2)$
    - $P(x) = (x - 1)(x + 1)(x - 2)$
    - $P(x) = x(x - 3)(x + 2)$
    - $P(x) = (2x - 1)(x + 1)(x + 3)$
    - $P(x) = (x - 3)(x + 2)(3x - 2)$
    - $P(x) = \frac{1}{5}x(x - 5)^2$
    - $P(x) = (x - 1)^2(x - 3)$
    - $P(x) = \frac{1}{4}(x + 1)^3(x - 3)$
    - $P(x) = \frac{1}{12}(x + 2)^2(x - 3)^2$
    - $P(x) = (x - 1)^2(x + 2)^3$
    - $P(x) = x^3(x + 2)(x - 3)^2$
    - $P(x) = (x - 3)^2(x + 1)^2$

4. Factor the polynomial and use the factored form to find the zeros. Then sketch the graph.
    - $P(x) = x^3 - x^2 - 6x$
    - $P(x) = x^3 + 2x^2 - 8x$
    - $P(x) = -x^3 + x^2 + 12x$
    - $P(x) = -2x^3 - x^2 + x$
    - $P(x) = x^4 - 3x^3 + 2x^2$
    - $P(x) = x^5 - 9x^3$
    - $P(x) = x^3 + x^2 - x - 1$
    - $P(x) = x^3 + 3x^2 - 4x - 12$
    - $P(x) = 2x^3 - x^2 - 18x + 9$
    - $P(x) = \frac{1}{8}(2x^4 + 3x^3 - 16x - 24)$
    - $P(x) = x^4 - 2x^3 - 8x + 16$
    - $P(x) = x^4 - 2x^3 + 8x - 16$
    - $P(x) = x^4 - 3x^2 - 4$
    - $P(x) = x^6 - 2x^3 + 1$

5. Determine the end behavior of P. Compare the graphs of P and Q on large and small viewing rectangles.
    - $P(x) = 3x^3 - x^2 + 5x + 1; Q(x) = 3x^3$
    - $P(x) = -\frac{1}{8}x^3 + \frac{1}{4}x^2 + 12x; Q(x) = -\frac{1}{8}x^3$
    - $P(x) = x^4 - 7x^2 + 5x + 5; \quad Q(x) = x^4$
    - $P(x) = -x^5 + 2x^2 + x; \quad Q(x) = -x^5$
    - $P(x) = x^{11} - 9x^9; \quad Q(x) = x^{11}$
    - $P(x) = 2x^2 - x^{12}; \quad Q(x) = -x^{12}$

6. The graph of a polynomial function is given. From the graph, find
    1. the x- and $y$-intercepts.
    2. the coordinates of all local extrema.

    - $P(x) = -x^2 + 4x$
    - $P(x) = \frac{2}{9}x^3 - x^2$
    - $P(x) = -\frac{1}{2}x^3 + \frac{3}{2}x - 1$
    - $P(x) = \frac{1}{9}x^4 - \frac{4}{9}x^3$

7. Graph the polynomial in the given viewing rectangle. Find the coordinates of all local extrema. State each answer correct to two decimal places.
    - $y = -x^2 + 8x, \quad [-4, 12] \text{ by } [-50, 30]$
    - $y = x^3 - 3x^2, \quad [-2, 5] \text{ by } [-10, 10]$
    - $y = x^3 - 12x + 9, \quad [-5, 5] \text{ by } [-30, 30]$
    - $y = 2x^3 - 3x^2 - 12x - 32, \quad [-5, 5] \text{ by } [-60, 30]$
    - $y = x^4 + 4x^3, \quad [-5, 5] \text{ by } [-30, 30]$
    - $y = x^4 - 18x^2 + 32, \quad [-5, 5] \text{ by } [-100, 100]$
    - $y = 3x^5 - 5x^3 + 3, \quad [-3, 3] \text{ by } [-5, 10]$
    - $y = x^5 - 5x^2 + 6, \quad [-3, 3] \text{ by } [-5, 10]$

8. Graph the polynomial and determine how many local maxima and minima it has.
    - $y = -2x^2 + 3x + 5$
    - $y = x^3 + 12x$
    - $y = x^3 - x^2 - x$
    - $y = 6x^3 + 3x + 1$
    - $y = x^4 - 5x^2 + 4$
    - $y = 1.2x^5 + 3.75x^4 - 7x^3 - 15x^2 + 18x$
    - $y = (x - 2)^5 + 32$
    - $y = (x^2 - 2)^3$
    - $y = x^8 - 3x^4 + x$
    - $y = \frac{1}{3}x^7 - 17x^2 + 7$

9. Graph the family of polynomials in the same viewing rectangle, using the given values of c. Explain how changing the value of c affects the graph.
    - $P(x) = cx^3; \quad c = 1, 2, 5, \frac{1}{2}$
    - $P(x) = (x - c)^4; \quad c = -1, 0, 1, 2$
    - $P(x) = x^4 + c; \quad c = -1, 0, 1, 2$
    - $P(x) = x^3 + cx; \quad c = 2, 0, -2, -4$
    - $P(x) = x^4 - cx; \quad c = 0, 1, 8, 27$
    - $P(x) = x^c; \quad c = 1, 3, 5, 7$

10. On the same coordinate axes,
    1. sketch graphs of the functions $y = x^3 - 2x^2 - x + 2$ and $y = -x^2 + 5x + 2$.
    2. Based on your sketch in part (a), at how many points do the two graphs appear to intersect?
    3. Find the coordinates of all intersection points.

11. Recall that a function f is odd if $f(-x) = -f(x)$ or even if $f(-x) = f(x)$ for all real x.
    1. Show that a polynomial P(x) that contains only odd powers of x is an odd function.
    2. Show that a polynomial P(x) that contains only even powers of x is an even function.
    3. Show that if a polynomial P(x) contains both odd and even powers of x, then it is neither an odd nor an even function.
    4. Express the function $P(x) = x^5 + 6x^3 - x^2 - 2x + 5$ as the sum of an odd function and an even function.

12.
    1. Graph the function $P(x) = (x - 1)(x - 3)(x - 4)$ and find all local extrema, correct to the nearest tenth.
    2. Graph the function $Q(x) = (x - 1)(x - 3)(x - 4) + 5$ and use your answers to part (a) to find all local extrema, correct to the nearest tenth.

13.
    1. Graph the function $P(x) = (x - 2)(x - 4)(x - 5)$ and determine how many local extrema it has.
    2. If $a < b < c$, explain why the function $P(x) = (x - a)(x - b)(x - c)$ must have two local extrema.

14.
    1. How many x-intercepts and how many local extrema does the polynomial $P(x) = x^3 - 4x$ have?
    2. How many x-intercepts and how many local extrema does the polynomial $Q(x) = x^3 + 4x$ have?
    3. If $a > 0$, how many $x$-intercepts and how many local extrema does each of the polynomials $P(x) = x^3 - ax$ and $Q(x) = x^3 + ax$ have? Explain your answer.

15. A market analyst working for a small-appliance manufacturer finds that if the firm produces and sells x blenders annually, the total profit (in dollars) is $P(x) = 8x + 0.3x^2 - 0.0013x^3 - 372$. Graph the function P in an appropriate viewing rectangle and use the graph to answer the following questions.
    - When just a few blenders are manufactured, the firm loses money (profit is negative). (For example, $P(10) = -263.3$, so the firm loses 263.30 if it produces and sells only 10 blenders.) How many blenders must the firm produce to break even?
    - Does profit increase indefinitely as more blenders are produced and sold? If not, what is the largest possible profit the firm could have?

16. The rabbit population on a small island is observed to be given by the function $P(t) = 120t - 0.4t^4 + 1000$ where t is the time (in months) since observations of the island began.
    - When is the maximum population attained, and what is that maximum population?
    -  When does the rabbit population disappear from the island?

17. An open box is to be constructed from a piece of cardboard 20cm by 40cm by cutting squares of side length x from each corner and folding up the sides, as shown in the figure.
    - Express the volume V of the box as a function of x.
    - What is the domain of V? (Use the fact that length and volume must be positive.)
    - Draw a graph of the function V and use it to estimate the maximum volume for such a box.

18. A cardboard box has a square base, with each edge of the base having length x inches, as shown in the figure. The total length of all 12 edges of the box is 144in.
    - Show that the volume of the box is given by the function $V(x) = 2x^2(18 - x)$.
